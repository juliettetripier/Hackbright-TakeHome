const timeSlotButtons = document.querySelectorAll('.time-slot-button')

function bookAppointment(evt) {

    let timeSlot = evt.target.value;

    const formInputs = {
        timeslot: document.querySelector 
    }
}